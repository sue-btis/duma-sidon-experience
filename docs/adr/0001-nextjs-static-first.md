# Use Next.js with static export initially

Ecosat's public site will use Next.js and TypeScript, exported as static assets. This supports the requested React ecosystem while keeping the initial deployment free of an application backend; a server capability may be introduced only when a confirmed requirement needs it.
