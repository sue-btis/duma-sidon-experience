# Keep translation messages modular by feature

Each locale stores separate message files for shared and feature-specific copy, which are dynamically aggregated into the locale's message object. This keeps translations adjacent to their concern as Ecosat's product-family pages grow; an authentication message module is intentionally absent because the site has no authentication.
